# CPU Scheduling Simulation

This project implements FCFS, SJF, and SRTF scheduling algorithms in Java to compare their performance (Waiting Time and Turnaround Time).

## How to Run
1. Open terminal.
2. Compile: `javac SchedulingApp.java`
3. Run: `java SchedulingApp`
